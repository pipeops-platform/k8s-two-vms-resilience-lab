---
- name: Verify cluster basic health
  hosts: control
  become: true
  tasks:
    - name: List cluster nodes
      command: kubectl get nodes -o wide
      environment:
        KUBECONFIG: /etc/kubernetes/admin.conf
      register: nodes
      changed_when: false

    - name: Print nodes output
      debug:
        var: nodes.stdout_lines
